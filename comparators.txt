auto cmp = [](const pp & a, const pp & b) {
    if (a.first != b.first) return a.first < b.first;
    return a.second < b.second;
};

set<pp, decltype(cmp)> st(cmp);


auto cmp = [](const pp& a, const pp& b) {
	if (a.first != b.first) return a.first > b.first;
	return a.second > b.second;
};

priority_queue<pp,vector<pp>,decltype(cmp)> pq(cmp);
